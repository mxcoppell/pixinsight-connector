node_modules/
.env
.DS_Store

# local scratch: architecture notes, plans, anything not for the repo
local/

# a session run from the checkout writes here (the connector's state dir and deliverables)
/agentic/
/output/

# local-only: terms that must never appear in this repo (private skill technique, test datasets); read by test/no-leak.test.mjs
.denylist
