## What changed

<!-- One or two sentences. Link the issue if there is one. -->

## Gates run

- [ ] `npm test` passes locally (Node version: )
- [ ] `npm run docs:tools` re-run if a tool was added or renamed

## Tested against a live PixInsight?

- [ ] Yes: PixInsight version , OS
- [ ] No: say what could not be verified without PixInsight

## Separation rule

- [ ] No processing opinion added: no recommended values, no ordering, no "use this next" in any tool
      description (`test/contract.test.mjs` passes)
